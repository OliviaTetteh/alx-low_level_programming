#include "main.h"

/**
 * _isupper - This code checks if a letter is uppercase
 * @x: The number to be checked
 * return: 1 for uppercase letter or 0 for any else
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
