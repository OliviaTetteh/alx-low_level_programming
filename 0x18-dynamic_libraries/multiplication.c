#include <stdlib.h>
#include <stdio.h>

/**
 * * mul - it multiplies the integers
 * @j: first int
 * @k: second int
 * Return: multiplied final results
 */

int mul(int j, int k)
{
        return (j * k);
}
