#include "main.h"
#include <stdio.h>

/**
 * main - It will check the code
 * Return: always 0
 */

int main(void)
{
	int n;

		n = 402;
		printf("n=%dn", n);
		reset_to_98(&n);
		printf("n=%d\n", n);
		return (0);
}

































































































































